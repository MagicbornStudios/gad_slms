# Reports across the board cadence

User wants 'reports across the board' as we build out training/eval. Existing: experiments/REPORT.md (sweep narrative), experiments/INDEX.md (per-run ledger). Add: (1) per-checkpoint REPORT.md inside experiments/runs/<name>/ summarizing config + scores + sample completions; (2) cross-benchmark scoreboard auto-regenerated by scripts/sweep_report.py covering GAD-tools + HumanEval + GSM8K + reasoning-quality; (3) decision-log to README.md mapping each numbered decision to its impact on the codebase. All reports are markdown, committed alongside the artifacts that produced them.
