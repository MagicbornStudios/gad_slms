# trigger_skill envelope spec — framework handoff

Per slm-learning-128 + pressure_formula_v2.md, s_skill is silently zero because .planning/.provenance/*.jsonl events never carry a trigger_skill field. The provenance writer is the gad CLI hook-runtime (PreToolUse/PostToolUse, installed via 'gad install hooks --claude'), distributed as a compiled binary at C:\Users\benja\AppData\Local\Programs\gad\bin\gad.exe — canonical source MagicbornStudios/get-anything-done, NOT in this repo. Spec at reports/research/trigger_skill_envelope_spec.md proposes an optional top-level {id, kind, depth, started_ts} object on each provenance event, populated when a Skill tool call or slash-command is on the active-skill stack. Until the gad release lands, w_skill = 0 in v2 formula (already done). Acceptance test: skill_pressure_correlation.py must report at least one nonzero invocation count after a session with Skill tool calls. Cross-project handoff required.
