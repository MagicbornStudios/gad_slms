# Gad-site landing page: north-star training target

User confirmed the gad-site that ships with every GAD project is the north-star quality bar for 'this model can actually code.' Action items to make this real: (1) inventory the gad-site source (Next.js routes, components, copy patterns, theme), (2) extract (prompt, file, body) training pairs from each component file — 'create a landing page hero', 'create a phases card', etc., (3) write a structural-diff eval that scores model output against a reference scaffold (file presence, route shape, component nesting depth, Tailwind class coverage), (4) target: 70% structural coverage from a single 'scaffold a landing site for project X' prompt. Until then, treat coding capability as 'can produce parseable Python' (current bar — see eval_humaneval.json: errors moved from SyntaxError to NameError after Stage 2.5).
