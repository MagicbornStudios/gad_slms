{
  "run_id": "morphism-0p5b-variant-a-lr5e5-2026-05-08",
  "exp_id": "EXP-009-morphism-projection-lr5e5",
  "purpose": "Re-fire of morphism Variant A on Qwen2.5-Coder-0.5B-Instruct at lr=5e-5 (4× smaller than the lr=2e-4 first run that catastrophically regressed). Tests whether the slm-learning-170 falsification was hyperparameter-driven (the +4.8pp morphism-over-LoRA HE delta in the first run hints that the architecture itself isn't broken). Same 73-row 0.5B-base-failure dataset, same insert position, same epochs.",
  "base_model": "Qwen/Qwen2.5-Coder-0.5B-Instruct",
  "dataset_volume_path": "/data/processed/0p5b-hard-fn-norm-2026-05-08/rows.jsonl",
  "system_prompt": "You are a coding assistant. Solve the problem in clean Python.",
  "instruction_field": "input",
  "response_field": "target",
  "max_length": 1024,
  "morphism": {
    "variant": "A_identity_projection",
    "insert_after_layer": 11,
    "init_check": {
      "min_agreement": 0.99,
      "max_new_tokens": 16,
      "prompts": [
        "def hello():",
        "import math\nfrom typing import List",
        "def fibonacci(n: int) -> int:",
        "class Stack:",
        "for i in range(10):",
        "def is_prime(n):",
        "x = [1, 2, 3]",
        "def reverse_string(s: str) -> str:"
      ]
    }
  },
  "training": {
    "epochs": 3,
    "lr": 5e-5,
    "batch_size": 4,
    "grad_accum": 4,
    "warmup_ratio": 0.05,
    "bf16": true,
    "gradient_checkpointing": false,
    "seed": 42,
    "logging_steps": 5,
    "max_grad_norm": 1.0
  },
  "gpu": "A10G",
  "timeout_seconds": 7200,
  "hub_repo": null,
  "decision_refs": ["slm-learning-130", "slm-learning-165", "slm-learning-169", "slm-learning-170"],
  "notes": "Single-arm follow-up to slm-learning-170. Pass criterion: HE within ±5pp of bare 0.5B base (vs the −28pp seen at lr=2e-4). If passes, Variant A's regression at lr=2e-4 is hyperparameter-driven not architectural; pure-architectural test wins. If fails, Variant A is genuinely broken at 0.5B and the lane closes."
}
